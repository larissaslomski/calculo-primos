# calculo-primos
A implementação do Backend foi feita em Python e o Front-End foi implementado em React. Foi utilizado o Flask para integração do Back-end com Front-end.

Como rodar
Python backend e React frontend
npx install 
pip3 install Flask